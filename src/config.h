#pragma once

enum class BallType
{
    Gray,
    Blue
};

enum class BrickType
{
    Gray,
    Blue,
    Green,
    Yellow,
    Red,
    Purple
};

enum class PaddleType
{
    Blue,
    Red
};